# Hypothesis records

Each hypothesis file is a projection. The planned experiment document under `docs/experiments/` owns predictions.

Copy this directory with the campaign template. Add one file per hypothesis after the operator copies the template into `lab/campaigns/<slug>/`.

Do not store predictions here.
