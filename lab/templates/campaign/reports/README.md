# Reports

Human-readable summaries live here after synthesis. Trial detail stays in the ledger.

A report must link the campaign, the evaluator lock digest, and the experiment documents. It must not replace those files.
