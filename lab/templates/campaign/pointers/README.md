# Artifact pointers

Tracked pointer files name ignored artifacts under `ignored/research/<slug>/`.

Each pointer records a path, a sha256 digest, and a recorded time. The verifier checks pointer syntax. It does not require the ignored bytes to exist in CI.
