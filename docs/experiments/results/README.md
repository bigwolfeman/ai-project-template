# Results artifacts

One directory per run: `yyyy-mm-dd-slug/`, matching the experiment markdown slug.

Include a short `README.md` in that directory listing files and how they were produced. Large binaries belong in `ignored/experiment-artifacts/` with a pointer here.

Do not leave unlabeled dumps.
